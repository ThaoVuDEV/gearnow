name: Deploy to VPS (self-hosted)

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Install PHP deps
        run: |
          cd /var/www/gearnow
          git pull origin main
          npm run build

      

      
